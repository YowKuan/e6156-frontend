version: 0.2

phases:
  pre_build:
    commands:
      - cd front-end-6156
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
  discard-paths: no
  base-directory: build