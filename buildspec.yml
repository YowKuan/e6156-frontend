version: 0.2

phases:
  pre_build:
    commands:
      - cd front-end-6156
      - npm config set registry https://registry.npmjs.com/
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
  discard-paths: no
  base-directory: front-end-6156/build